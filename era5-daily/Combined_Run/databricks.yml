# This is a Databricks asset bundle definition for combined_run.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: combined_run

include:
  - resources/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    workspace:
      host: https://dbc-ad3d47af-affb.cloud.databricks.com

    # run_as:
    #   This runs as smajumder1@ua.edu in production. We could also use a service principal here,
    #   see https://docs.databricks.com/dev-tools/bundles/permissions.html.
    #   user_name: "0dbc3b87-0681-43d4-b52e-53bce65fcae5"

  # Optionally, there could be a 'staging' target here.
  # (See Databricks docs on CI/CD at https://docs.databricks.com/dev-tools/bundles/ci-cd.html.)
  #
  # staging:
  #   workspace:
  #     host: https://dbc-ad3d47af-affb.cloud.databricks.com

  # The 'staging' target, used for staging deployment.
  staging:
    mode: production
    workspace:
      host: https://dbc-59ffb06d-e490.cloud.databricks.com
    run_as:
      service_principal_name: "0dbc3b87-0681-43d4-b52e-53bce65fcae5"

  # The 'prod' target, used for production deployment.
  prod:
    # We use 'mode: production' to indicate this is a production deployment.
    # Doing so enables strict verification of the settings below.
    mode: production
    workspace:
      host: https://dbc-59ffb06d-e490.cloud.databricks.com
      # We always use /Users/smajumder1@ua.edu for all resources to make sure we only have a single copy.
      # If this path results in an error, please make sure you have a recent version of the CLI installed.
      root_path: /Users/smajumder1@ua.edu/.bundle/${bundle.name}/${bundle.target}
    # run_as:
    #   This runs as smajumder1@ua.edu in production. We could also use a service principal here,
    #   see https://docs.databricks.com/dev-tools/bundles/permissions.html.
    #   user_name: "0dbc3b87-0681-43d4-b52e-53bce65fcae5"
